{
  "name": "@quanti/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  }
}
