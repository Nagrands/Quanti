{
  "name": "@quanti/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/main.tsx",
    "tauri:check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/**/*.test.mjs"
  }
}
